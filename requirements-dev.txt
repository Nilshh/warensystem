# Nur für Tests/Entwicklung — nicht im Container installiert.
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#   .venv/bin/pytest
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
